# 财报智能问数系统——零基础完全指南

> 从一个PDF文件开始，到用户问"金花股份2022年净利润多少"能回答出来，这条完整的路是怎么走的。

---

## 1. 这是什么项目

泰迪杯第十四届数据挖掘竞赛B题：**构建一个上市公司财报智能问数系统**。

简单说：给你一堆上市公司的年报PDF，系统自动把财务数据抽出来存到数据库里，然后用户用中文自然语言提问（比如"2022年净利润最高的3家公司"），系统生成SQL查数据库，返回答案+图表+结论。

**核心价值：** 不是调API的玩具——从PDF解析到NLP到前后端开发，是一套完整的数据产品。

---

## 2. 整体架构：数据怎么流

```
┌──────────┐    ┌──────────────┐    ┌──────────────┐    ┌──────────┐
│ 1252份   │───→│ 双引擎提取    │───→│ raw_extracted │───→│ 字段映射  │
│ PDF年报  │    │ 规则+LLM兜底  │    │ (原始数据)    │    │          │
└──────────┘    └──────────────┘    └──────────────┘    └──────────┘
                                                               │
                                                               ▼
┌──────────┐    ┌──────────────┐    ┌──────────────┐    ┌──────────┐
│ 答案+图表 │←───│ 可视化+结论  │←───│ SQL执行      │←───│ 四张标准表│
│          │    │              │    │              │    │          │
└──────────┘    └──────────────┘    └──────────────┘    └──────────┘
     ↑                                    ↑
     │                                    │
┌────┴─────┐    ┌──────┐    ┌────────┐   ┌─┴────────┐
│ 用户问句 │→→→→│预处理│→→→→│意图分类│→→→│ 槽位填充 │
│          │    │      │    │        │   │          │
└──────────┘    └──────┘    └────────┘   └────┬─────┘
                                              │
                          ┌───────────────────┘
                          ▼
                    ┌──────────┐
                    │ NL2SQL   │
                    │ 自然语言→SQL│
                    └────┬─────┘
                         │
                    ┌────▼─────┐
                    │ SQL校验  │
                    │ 安全拦截 │
                    └──────────┘
```

**数据管道分两段：**
- **离线（数据入库）**：PDF → 提取 → 映射 → 校验 → 四张表
- **在线（问答）**：问句 → 预处理 → 意图 → 槽位 → NL2SQL → 执行 → 可视化

---

## 3. 技术栈一览

| 层 | 技术 | 为什么选它 |
|---|------|-----------|
| PDF解析 | pdfplumber | 比pymupdf对复杂表格更友好，能拿到每个字符的坐标 |
| LLM兜底 | DeepSeek-V3 (SiliconFlow代理) | 便宜，中文强，代理端点免备案 |
| 意图分类 | TF-IDF + 逻辑回归 | 样本少（1708条），传统ML比深度学习泛化好 |
| 槽位填充 | 规则匹配 | 准确率够用，零训练成本 |
| NL2SQL | Prompt Engineering + Few-Shot | 不需要微调，prompt里放6个示例就够 |
| 数据库 | MySQL 8 | 结构化数据，支持复杂查询+窗口函数 |
| 后端 | FastAPI (Python) | 异步支持好，自动生成API文档 |
| 前端 | React + TypeScript + Vite | 组件化开发，热重载快 |
| 图表 | Matplotlib + ECharts | 后端生成静态图，前端交互图表 |
| 向量检索 | BAAI/bge-large-zh-v1.5 + ChromaDB | 中文embedding效果最好的开源模型之一 |

---

## 4. 项目文件结构（每个文件干什么）

```
任务B_财报智能抽取/
│
├── 📊 数据管道（离线）
│   ├── extractors/
│   │   ├── engine_a_rules.py       ⭐ 规则引擎：pdfplumber提取表格数据
│   │   ├── engine_b_deepseek.py    LLM兜底：API提取规则搞不定的数据
│   │   ├── pdf_extractor.py        统一入口：调度双引擎
│   │   └── table_mapper.py         表类型映射：判断数据属于哪张表
│   ├── run_batch_extract.py        批量处理所有PDF
│   ├── field_matcher.py            ⭐ 字段映射：中文术语→数据库字段名
│   ├── llm_filler.py               LLM填充规则缺漏的字段
│   └── validator.py               ⭐ 五维校验：表内平衡/钩稽/时序/业务逻辑/跨报
│
├── 🧠 智能问答（在线）
│   ├── preprocess_pipeline.py      文本预处理（全角半角、术语标准化）
│   ├── predict_intent.py           意图分类推理（加载训练好的模型）
│   ├── train_intent_classifier.py  训练意图分类器（TF-IDF+LR）
│   ├── slot_filler.py              ⭐ 槽位填充：抽取公司/年份/指标
│   ├── slot_filler_bert.py         BERT版槽位填充（代码完整，待GPU训练）
│   ├── nl2sql.py                  ⭐ NL2SQL：自然语言→SQL生成
│   ├── sql_validator.py           SQL安全校验（防注入）
│   ├── knowledge_graph.py         ⭐ 知识图谱：派生指标推理（ROE=净利/净资产）
│   ├── visualizer.py              可视化+结论生成
│   └── pipeline.py                ⭐ 总调度：串联7个模块的端到端流水线
│
├── 🔍 RAG检索增强（任务三）
│   ├── rag_knowledge_base.py       文档切分+向量化+混合检索
│   ├── entity_aligner.py           实体对齐（查询词→数据库实体）
│   ├── agent_planner.py            Plan-and-Execute多步推理Agent
│   ├── attribution_module.py       归因分析模块
│   └── task3_pipeline.py           任务三统一入口
│
├── 🌐 后端服务
│   ├── app.py                      FastAPI入口（/chat /sql /tables等路由）
│   ├── config_loader.py            加载.env配置文件
│   └── start_backend.bat           Windows一键启动脚本
│
├── 🖥️ 前端
│   └── frontend/
│       ├── src/
│       │   ├── pages/Chat/         智能问答页（含思考过程展示）
│       │   ├── pages/Login/        用户登录页
│       │   ├── pages/Database/     数据库浏览页
│       │   ├── pages/KnowledgeBase/知识库页
│       │   ├── components/Layout/  侧边栏布局
│       │   ├── services/api.ts     Axios API封装
│       │   ├── store/              Zustand状态管理
│       │   ├── types/              TypeScript类型定义
│       │   └── utils/              工具函数（格式化等）
│       ├── vite.config.ts          Vite配置（代理/api→后端8000）
│       └── package.json            依赖清单
│
├── 📦 数据与配置
│   ├── .env                        ⚠️ 密码/API Key（不进Git）
│   ├── .gitignore                  忽略规则
│   ├── config/                     财务术语映射表
│   └── data/
│       ├── company_alias.json      公司名→股票代码映射
│       ├── financial_dictionary.json 财务术语标准词典
│       ├── intent_train.csv        意图分类训练数据
│       ├── slot_train.jsonl        槽位填充训练数据
│       ├── subject_synonym.json    科目同义词
│       └── time_dict.json          时间表达标准化
│
└── 📖 文档
    ├── README.md                   项目说明
    └── 项目完全指南.md              本文档
```

---

## 5. 核心模块详解

### 5.1 PDF数据提取（engine_a_rules.py）

**要干什么：** 从年报PDF表格中抽取财务数据

**为什么难：**
- 上交所和深交所的PDF表格结构不一样
- 同一家公司不同年份的PDF结构也可能不同
- 表格有合并单元格、跨页、水印干扰

**怎么做：**
1. 用pdfplumber提取表格 + 字符坐标
2. 识别表头（年份标签如"2022年"在其他列偏移位置）
3. 双向扫描匹配数据（优先向右找数值，找不到再向左）
4. 推断报告类型（资产负债表/利润表/现金流量表/核心指标表）

**关键创新——自适应列对齐：**
深交所年报的年份标签在奇数列、数值在偶数列，存在列偏移。规则引擎不再固定取"年份列+1"，而是双向扫描取第一个非空数值。

**成功率：** 1252份年报中86%能自动提取，剩下14%是极窄表格（2-3列）或扫描件。

### 5.2 字段映射（field_matcher.py）

**要干什么：** 把PDF提取的"营业收入（元）"映射到数据库字段 `total_operating_revenue`

**为什么难：**
- 同一财务指标在不同报表中有不同叫法（"营业总收入" vs "营业收入" vs "主营业务收入"）
- 不同公司用词习惯不同

**怎么做：**
1. 维护一个中文术语→数据库字段的映射字典
2. 支持模糊匹配（编辑距离+关键词匹配）
3. 按字段专属程度决定目标表（不信任自动判别的auto_table_type）
4. 批量处理，支持 `--reprocess failed` 重试失败记录

### 5.3 五维校验（validator.py）

**要干什么：** 数据入库前的质量把关

**五个维度：**

| 维度 | 检查什么 | 例子 |
|------|---------|------|
| 表内平衡 | 会计恒等式 | 资产 = 负债 + 所有者权益 |
| 表间钩稽 | 跨表一致性 | 利润表净利润 = 资产负债表未分配利润变动 |
| 时序连续性 | 增长率合理性 | 营收年增长率不在-80%~300%范围就报警 |
| 业务逻辑 | 常识约束 | 净利润不能大于营业收入 |
| 跨报告一致 | 年报 vs 季报 | 冲突以年报为准 |

### 5.4 意图分类（predict_intent.py）

**要干什么：** 判断用户想干什么——查询、计算、对比、排名还是多条件查询

**五类意图：**

| 类别 | 例子 | 生成什么SQL |
|------|------|-----------|
| QUERY_SINGLE | "金花股份2022年净利润" | SELECT ... WHERE stock_code=... |
| CALCULATE | "金花股份2022年毛利率" | SELECT ... WHERE ... + 计算列 |
| COMPARE | "金花和万邦德2022年营收对比" | SELECT ... WHERE stock_code IN (...) |
| RANK | "2022年净利润最高的3家" | SELECT ... ORDER BY ... LIMIT 3 |
| QUERY_MULTI | "金花股份2022年营收和净利润" | SELECT ... WHERE ... + 多字段 |

**技术方案：** TF-IDF向量化 + 逻辑回归分类。1708条训练数据，5折交叉验证F1=0.80。

**为什么没用BERT：** 样本量小（~1700条），传统ML不容易过拟合。实践中TF-IDF+LR的泛化性能更好。

### 5.5 槽位填充（slot_filler.py）

**要干什么：** 从问句中提取结构化信息

**输入：** "金花股份2022年净利润是多少"
**输出：** `{company: "金花股份", company_code: "600080", year: "2022", metric: "净利润"}`

**三个槽位：**
- 公司名 → 股票代码（查company_alias.json）
- 年份 → 标准化为YYYY或YYYY-MM-DD格式
- 财务指标 → 标准化术语（查financial_dictionary.json）

### 5.6 NL2SQL（nl2sql.py）

**要干什么：** 自然语言 + 槽位信息 → 可执行的SQL

**核心思路：** Prompt Engineering，不是微调模型。在prompt里注入：
- 数据库Schema（四张表的字段）
- 6个Few-Shot示例（每种意图至少一个例子）
- 安全规则（禁止DROP/DELETE/INSERT/UPDATE）
- 当前问句的槽位信息（公司代码、年份等）

**为什么不微调：** Prompt就够了，而且schema会变（加字段、改表名），用prompt比重新训练灵活得多。

### 5.7 知识图谱（knowledge_graph.py）

**要干什么：** 推理不能直接从数据库查到的指标

**例子：**
- 用户问"ROE" → 数据库中只有净利润和净资产 → 知识图谱知道 ROE = 净利润 / 净资产
- 用户问"资产负债率" → 数据库中只有总资产和总负债 → 知识图谱知道 资产负债率 = 总负债 / 总资产

**实现方式：** 规则推理 + 递归展开。ROE展开为 `net_profit / total_equity`，total_equity再展开为 `asset_total_assets - liability_total_liabilities`。

### 5.8 Plan-and-Execute Agent（agent_planner.py）

**要干什么：** 处理单步SQL搞不定的复杂问题

**场景：** "2022年净利润最高的3家公司，并计算它们的同比增速"

单条SQL做不到"查Top3 + 查上年数据 + 计算同比"三件事。Plan-and-Execute把它拆成：
1. PLAN：先查Top3 → 再查它们2021年数据 → 计算同比
2. EXECUTE：执行第1步SQL → 拿结果 → 构造第2步SQL → 执行
3. 汇总结果

### 5.9 RAG知识库（rag_knowledge_base.py）

**要干什么：** 让系统能回答"数据库中查不到"的问题

**场景：** "医药行业2022年整体毛利率趋势如何？"——这不在数据库中，但在160份研报PDF里

**流程：**
1. 研报PDF → marker解析 → 语义切分 → BAAI/bge-large-zh-v1.5向量化 → ChromaDB存储
2. 用户问→同样的模型向量化→混合检索（Dense向量+BM25关键词）→重排→返回Top-K
3. 把检索到的段落注入LLM上下文回答问题

---

## 6. 数据库设计

### raw_extracted（原始提取表）
```sql
serial_number INT AUTO_INCREMENT PRIMARY KEY,
pdf_name VARCHAR(255),
company_name VARCHAR(100),
report_period VARCHAR(20),    -- '2022-12-31'
raw_columns JSON,              -- 中文列名数组
raw_data JSON,                 -- 提取的原始数据
auto_table_type VARCHAR(50),   -- 自动推断的表类型（不可信！）
process_status VARCHAR(20),    -- pending/processed/failed
```

### 四张标准表

| 表名 | 存什么 | 代表字段 |
|------|-------|---------|
| balance_sheet | 资产负债表 | asset_total_assets（总资产）, liability_total_liabilities（总负债） |
| stock_income_statement_data | 利润表 | total_operating_revenue（营收）, net_profit（净利润） |
| income_sheet | 现金流量表 | cf_operating_cash_flow（经营活动现金流） |
| core_performance_indicators_sheet | 核心指标 | gross_profit_margin（毛利率）, net_profit_margin（净利率）, roe（ROE） |

---

## 7. 怎么跑起来

### 前提条件
- Python 3.9+
- Node.js 18+
- MySQL 8.0+
- Git

### 步骤

```bash
# 1. 克隆仓库
git clone https://github.com/1617914217-web/financial-report-chat.git
cd financial-report-chat

# 2. 配置 .env（复制 .env.example 或自己创建）
# MYSQL_HOST=127.0.0.1
# MYSQL_PORT=3306
# MYSQL_USER=root
# MYSQL_PASSWORD=你的密码
# MYSQL_DATABASE=intelligent_data_query
# SILICONFLOW_API_KEY=你的API Key

# 3. 安装Python依赖
pip install pdfplumber pymysql fastapi uvicorn scikit-learn sqlparse matplotlib

# 4. 启动后端
python -m uvicorn app:app --host 0.0.0.0 --port 8000 --reload

# 5. 启动前端（另开一个终端）
cd frontend
npm install
npm run dev
# → http://localhost:5173
```

### 数据处理（如果PDF还没提取）

```bash
python run_batch_extract.py        # 批量PDF提取
python field_matcher.py            # 字段映射
python field_matcher.py --reprocess failed  # 重试失败
python llm_filler.py               # LLM补缺
python validator.py                # 五维校验
```

---

## 8. 零基础怎么学会这个项目

**如果你完全零基础，按这个顺序学：**

### 第一周：看懂数据
1. 挑一家公司（比如金花股份600080），打开它的年报PDF，看懂三张表（资产负债表/利润表/现金流量表）是什么意思
2. 运行 `run_batch_extract.py` 提取一两份PDF，看 `raw_extracted` 表长什么样
3. 理解"字段映射"——PDF里的中文列名怎么对应数据库字段名

### 第二周：看懂流程
4. 读 `pipeline.py` 的 `ask()` 方法——7个步骤怎么串起来的
5. 单独调每个模块：先调 `preprocess_pipeline.py` 看预处理效果，再调 `predict_intent.py` 看意图分类，以此类推
6. 自己改几个测试问句，看每一步的输出变化

### 第三周：看懂关键模块
7. `engine_a_rules.py` 的列对齐算法——这是最难的PDF解析部分
8. `nl2sql.py` 的prompt设计——怎么用Few-Shot让LLM生成正确的SQL
9. `knowledge_graph.py` 的递归推理——派生指标怎么展开

### 第四周：前后端联调
10. 启动前后端，在浏览器里提问，看接口返回
11. 看前端 `ChatPage.tsx` 怎么展示思考过程
12. 改一下提示词、加一个财务指标、优化一下图表样式

---

## 9. 简历怎么写（面试用）

**Skills行：**
```
数据管道：PDF解析(pdfplumber)、NLP意图分类(TF-IDF+LR/F1=0.80)、NL2SQL、知识图谱推理
后端：FastAPI + MySQL | 前端：React+TypeScript+Vite
```

**项目描述（3条bullet）：**
> 泰迪杯B题·财报智能问数系统（队长） | 2026.04-05
> - 双引擎PDF解析（规则优先+LLM兜底），处理1252份年报，成功率86%——攻克深交所列错位难题
> - 7模块NLP问答Pipeline（意图分类→槽位填充→NL2SQL→五维校验→Plan-and-Execute），复杂查询自动拆解多步执行
> - FastAPI + React全栈开发，集成登录、思考过程可视化、图表自动生成

**面试会问什么：**
1. "深交所列错位怎么解决的？" → 双向扫描，优先向左取数
2. "为什么意图分类用TF-IDF不用BERT？" → 样本少，ML比DL泛化好
3. "五维校验是哪五维？" → 表内平衡/表间钩稽/时序/业务逻辑/跨报告
4. "Plan-and-Execute和ReAct有什么区别？" → P&E先计划再执行，ReAct边做边想
5. "86%成功率够不够工业级？" → 不够（需要99.9%），但思路是对的——知道哪14%不可信、标记出来

---

## 10. 这个项目的真实竞争力

### 强在哪里
- **不是调API的Demo**：从PDF底层的字符坐标解析开始做
- **完整闭环**：一个人搞定了数据采集→ETL→NLP→后端→前端
- **有真实的工程问题**：列错位、乱码、编码、数值精度——不是PPT项目
- **有成本意识**：规则优先LLM兜底，不是在炫技而是在省钱

### 弱在哪里
- 数据量级小（38家 vs 工业级5000+）
- 没有生产级的监控、告警、回滚
- 前端是功能Demo，不是产品级
- 没有分布式和性能优化经验

**面试官想要的人是什么样的？** 不是什么都懂的全栈大神，而是一个遇到问题能想清楚为什么、怎么解决、踩了什么坑的人。这个项目能证明这一点。

---

## 11. 踩过的坑（经验教训）

1. **深交所列错位**：年份标签在奇数列，数据在偶数列。一开始直接取标签列+1，全是空值。改双向扫描后解决。

2. **raw_extracted表的auto_table_type不可信**：系统自动推断的表类型经常错，因为一页PDF可能混了多种数据。最终不信任这个字段，改按实际字段内容匹配目标表。

3. **百分比格式超限**：PDF里毛利率存的是85.23，MySQL字段是decimal(10,4)，超过9999就爆。值大于1自动/100转小数。

4. **编码噩梦**：公司名在数据库里UTF-8字节被当成latin1存，变成乱码。PowerShell显示层又是GBK，三层编码打架。最终全链路强制utf8mb4解决。

5. **DeepSeek API Key被拒**：官方接口一直401。换成SiliconFlow代理端点解决。

---

*最后更新：2026年6月*
